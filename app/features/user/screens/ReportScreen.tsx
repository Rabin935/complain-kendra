import CreateComplaintScreen from "../../complaints/screens/CreateComplaintScreen";

export default function ReportScreen() {
  // Render the CreateComplaintScreen directly since Report is the primary action
  return <CreateComplaintScreen />;
}
